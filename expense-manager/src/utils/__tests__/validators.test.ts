/** @generated AUTO-GENERATED FILE - safe to overwrite */
import {
  emailSchema,
  passwordSchema,
  loginSchema,
  registerSchema,
  changePasswordSchema,
  updateProfileSchema,
  expenseSchema,
  categorySchema,
  budgetSchema,
  filtersSchema,
} from '../validators';

describe('validators', () => {
  describe('emailSchema', () => {
    it('is defined', () => {
      expect(emailSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(emailSchema).toEqual(expectedValue);
    });
  });

  describe('passwordSchema', () => {
    it('is defined', () => {
      expect(passwordSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(passwordSchema).toEqual(expectedValue);
    });
  });

  describe('loginSchema', () => {
    it('is defined', () => {
      expect(loginSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(loginSchema).toEqual(expectedValue);
    });
  });

  describe('registerSchema', () => {
    it('is defined', () => {
      expect(registerSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(registerSchema).toEqual(expectedValue);
    });
  });

  describe('changePasswordSchema', () => {
    it('is defined', () => {
      expect(changePasswordSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(changePasswordSchema).toEqual(expectedValue);
    });
  });

  describe('updateProfileSchema', () => {
    it('is defined', () => {
      expect(updateProfileSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(updateProfileSchema).toEqual(expectedValue);
    });
  });

  describe('expenseSchema', () => {
    it('is defined', () => {
      expect(expenseSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(expenseSchema).toEqual(expectedValue);
    });
  });

  describe('categorySchema', () => {
    it('is defined', () => {
      expect(categorySchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(categorySchema).toEqual(expectedValue);
    });
  });

  describe('budgetSchema', () => {
    it('is defined', () => {
      expect(budgetSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(budgetSchema).toEqual(expectedValue);
    });
  });

  describe('filtersSchema', () => {
    it('is defined', () => {
      expect(filtersSchema).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(filtersSchema).toEqual(expectedValue);
    });
  });
});
