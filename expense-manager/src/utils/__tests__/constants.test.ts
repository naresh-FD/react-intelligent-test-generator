/** @generated AUTO-GENERATED FILE - safe to overwrite */
import {
  APP_NAME,
  APP_VERSION,
  API_BASE_URL,
  API_TIMEOUT,
  TOKEN_KEY,
  REFRESH_TOKEN_KEY,
  USER_KEY,
  THEME_KEY,
  PREFERENCES_KEY,
  DEFAULT_PAGE_SIZE,
  PAGE_SIZE_OPTIONS,
  DEFAULT_CURRENCY,
  SUPPORTED_CURRENCIES,
  CURRENCY_SYMBOLS,
  DEFAULT_CATEGORIES,
  RECURRENCE_OPTIONS,
  BUDGET_PERIOD_OPTIONS,
  DATE_FORMAT_OPTIONS,
  CHART_COLORS,
  ROUTES,
  QUERY_KEYS,
  TOAST_DURATION,
  PASSWORD_MIN_LENGTH,
  PASSWORD_REGEX,
} from '../constants';

describe('constants', () => {
  describe('APP_NAME', () => {
    it('is defined', () => {
      expect(APP_NAME).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(APP_NAME).toEqual(expectedValue);
    });
  });

  describe('APP_VERSION', () => {
    it('is defined', () => {
      expect(APP_VERSION).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(APP_VERSION).toEqual(expectedValue);
    });
  });

  describe('API_BASE_URL', () => {
    it('is defined', () => {
      expect(API_BASE_URL).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(API_BASE_URL).toEqual(expectedValue);
    });
  });

  describe('API_TIMEOUT', () => {
    it('is defined', () => {
      expect(API_TIMEOUT).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(API_TIMEOUT).toEqual(expectedValue);
    });
  });

  describe('TOKEN_KEY', () => {
    it('is defined', () => {
      expect(TOKEN_KEY).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(TOKEN_KEY).toEqual(expectedValue);
    });
  });

  describe('REFRESH_TOKEN_KEY', () => {
    it('is defined', () => {
      expect(REFRESH_TOKEN_KEY).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(REFRESH_TOKEN_KEY).toEqual(expectedValue);
    });
  });

  describe('USER_KEY', () => {
    it('is defined', () => {
      expect(USER_KEY).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(USER_KEY).toEqual(expectedValue);
    });
  });

  describe('THEME_KEY', () => {
    it('is defined', () => {
      expect(THEME_KEY).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(THEME_KEY).toEqual(expectedValue);
    });
  });

  describe('PREFERENCES_KEY', () => {
    it('is defined', () => {
      expect(PREFERENCES_KEY).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(PREFERENCES_KEY).toEqual(expectedValue);
    });
  });

  describe('DEFAULT_PAGE_SIZE', () => {
    it('is defined', () => {
      expect(DEFAULT_PAGE_SIZE).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(DEFAULT_PAGE_SIZE).toEqual(expectedValue);
    });
  });

  describe('PAGE_SIZE_OPTIONS', () => {
    it('is defined', () => {
      expect(PAGE_SIZE_OPTIONS).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(PAGE_SIZE_OPTIONS).toEqual(expectedValue);
    });
  });

  describe('DEFAULT_CURRENCY', () => {
    it('is defined', () => {
      expect(DEFAULT_CURRENCY).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(DEFAULT_CURRENCY).toEqual(expectedValue);
    });
  });

  describe('SUPPORTED_CURRENCIES', () => {
    it('is defined', () => {
      expect(SUPPORTED_CURRENCIES).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(SUPPORTED_CURRENCIES).toEqual(expectedValue);
    });
  });

  describe('CURRENCY_SYMBOLS', () => {
    it('is defined', () => {
      expect(CURRENCY_SYMBOLS).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(CURRENCY_SYMBOLS).toEqual(expectedValue);
    });
  });

  describe('DEFAULT_CATEGORIES', () => {
    it('is defined', () => {
      expect(DEFAULT_CATEGORIES).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(DEFAULT_CATEGORIES).toEqual(expectedValue);
    });
  });

  describe('RECURRENCE_OPTIONS', () => {
    it('is defined', () => {
      expect(RECURRENCE_OPTIONS).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(RECURRENCE_OPTIONS).toEqual(expectedValue);
    });
  });

  describe('BUDGET_PERIOD_OPTIONS', () => {
    it('is defined', () => {
      expect(BUDGET_PERIOD_OPTIONS).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(BUDGET_PERIOD_OPTIONS).toEqual(expectedValue);
    });
  });

  describe('DATE_FORMAT_OPTIONS', () => {
    it('is defined', () => {
      expect(DATE_FORMAT_OPTIONS).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(DATE_FORMAT_OPTIONS).toEqual(expectedValue);
    });
  });

  describe('CHART_COLORS', () => {
    it('is defined', () => {
      expect(CHART_COLORS).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(CHART_COLORS).toEqual(expectedValue);
    });
  });

  describe('ROUTES', () => {
    it('is defined', () => {
      expect(ROUTES).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(ROUTES).toEqual(expectedValue);
    });
  });

  describe('QUERY_KEYS', () => {
    it('is defined', () => {
      expect(QUERY_KEYS).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(QUERY_KEYS).toEqual(expectedValue);
    });
  });

  describe('TOAST_DURATION', () => {
    it('is defined', () => {
      expect(TOAST_DURATION).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(TOAST_DURATION).toEqual(expectedValue);
    });
  });

  describe('PASSWORD_MIN_LENGTH', () => {
    it('is defined', () => {
      expect(PASSWORD_MIN_LENGTH).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(PASSWORD_MIN_LENGTH).toEqual(expectedValue);
    });
  });

  describe('PASSWORD_REGEX', () => {
    it('is defined', () => {
      expect(PASSWORD_REGEX).toBeDefined();
    });

    it('has expected value', () => {
      // TODO: Verify the value
      // expect(PASSWORD_REGEX).toEqual(expectedValue);
    });
  });
});
